events {
    worker_connections 1024;
}

http {
    server {
        listen 80;

        location /metrics {
            default_type text/plain;
            alias /usr/share/nginx/html/metrics;
        }

        location / {
            return 200 'OK';
            default_type text/plain;
        }
    }
}
